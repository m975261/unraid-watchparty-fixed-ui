# Unraid Templates for watchparty-fixed-ui

XML templates for Unraid Community Applications.

## Install
Copy `my-watchparty-fixed-ui.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
